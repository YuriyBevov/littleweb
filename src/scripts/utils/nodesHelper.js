const burger = document.querySelector('.nav-opener');
const nav = document.querySelector('.nav');
const header = document.querySelector('header');
const footer = document.querySelector('footer');

export { header, burger, nav, footer };
